# 执行与验证记录

## 当前交付：仅电影与剧集，保留可配置候选判断器

- 工作区：`/Users/abc/.codex/worktrees/metadata-providers/kodi-metadata-tmdb-cli`。
- 分支：`codex/metadata-providers`；基于 master 的 `9f184d156acf7b58a08bd091d1b09ddde8472a0d`，保留原有领先远端的提交。
- 固定在线流程：通用 LLM 提取 → 所有已启用来源候选事实 → 配置选择的 Jev 或通用 LLM 判断 → NFO 与图片。
- 已删除旧匹配/搜索模式、规则解析、来源内部选择和首条回退。来源事实缓存不跳过模型。
- metadata.judge 允许 jev/llm，默认jev。llm复用ai配置且不要求Jev凭据；jev保留独立配置与Noul门槛。旧匹配/搜索模式仍被拒绝，没有自动切换服务。
- 两判断器共享相同紧凑事实证据，不由模型生成新元数据。
- 仅支持电影和剧集；已删除外部媒体探测、视频截图、音乐视频处理及其配置、扫描、监听和 Kodi 刷新入口。目录为空时不再绕过模型配置校验。

## 前轮验证（可配置候选判断器）

上一轮执行，当前移除版本的全量验证另行记录：

- `go test -race ./... -count=1 -timeout=60s -json`：13个有测试的包通过，114个测试函数、275个测试节点（含子测试），0失败。
- `go vet ./...`：通过。
- `git diff --check`：通过。
- 本机 macOS 构建：通过。
- `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build`：通过。
- `CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build`：通过。
- 配置文件 → 真实CLI → 本地提取及所选判断服务协议 → 实际Provider → NFO落盘：通过。
- 两来源已知编号直取、无编号全候选、Jev跨来源选择、同源节目/单集、原NFO保护与事实缓存回放：通过。
- 官方协议依据：docs.typesafe.ai 的 API、Choice、Noul、Confidence 与模型说明。

## 审查与修复

本轮可选判断器审查未发现重要问题。独立重跑common/ai、common/jev、metadata、providers、config均通过。新增CLI回归验证llm完全省略Jev配置和环境凭据，成功时使用所选真实候选，none/HTTP500时不覆盖原NFO且不改用Jev。


独立审查复现3个问题，均先建立失败回归再修复并复核：

1. Jev配置加载漏赋值：显式字段和环境变量均正确绑定。
2. LLM未知集号经join偏移制造有效坐标：换算前检查原季集已知，未知时不访问来源或写NFO。
3. 首个不符合人工group的候选阻断整个选择：候选约束不符可被排除，显式作品冲突和普通来源错误仍报错。

还验证了网站提供的同对象跨站编号保留在Jev证据中，不与LLM未验证的定位提示混为事实。

## 真实联调限制

- 本轮模型使用受控HTTP响应，未调用真实通用LLM或Jev账户；不能声明真实媒体样本准确率或门槛已校准。
- TheTVDB真实页面/公开搜索此前由curl取得，HTML样本解析通过；本机Go直连曾在连接阶段超时，真实组合CLI成功联调未完成。
- 未更改系统DNS/代理/防火墙或硬编码CDN，未访问真实媒体库；未验证真实TMDb凭据和播放器导入。

## 当前移除验证

- 模型配置回归先失败：空目录配置原先跳过模型校验；删除专用跳过分支后通过。
- 定向测试通过：全量扫描、电影/剧集当前目录扫描、监听派发、音频附件过滤、配置及两种判断器。
- `go test -race ./... -count=1 -timeout=60s -json`：14 个有测试的包、116 个测试函数、280 个测试节点全部通过，0 失败；结果保存为 `/tmp/metadata-movie-show-tests.jsonl`。
- `go vet ./...`、`git diff --check`：通过。macOS 本机构建、Linux amd64、Windows amd64 构建通过；产物位于 `/tmp/metadata-movie-show-*`。
- 受控来源及模型协议下的真实 CLI 电影/剧集输出回归通过；生产源码和示例 JSON 无外部媒体工具及音乐视频引用。
- README 及设计明确输出面向 Jellyfin、Emby、Silo Server、Kodi；四端真实导入仍未验收。

## 过程记录与可恢复性

- 本轮移除前完整差异保存在 `/tmp/metadata-before-movie-show-only.patch`。未删除任何用户媒体或历史缓存。
- 前一版多Provider结果的完整暂存差异保存在 `/tmp/metadata-providers-before-jev.patch`，未以旧83函数结果替代本轮验证。
- 本轮一次完整路由测试写入自动审批超时、未执行；按工具允许以独立测试文件重试一次后成功。没有绕过审批，也没有待处理审批阻塞。
- 最终代码、测试、示例配置、README、设计和报告保存在当前隔离工作区并暂存；未提交、合并或推送Git。

## 统一方案修订（2026-09-24）

- 复核附件、实际 Provider 搜索、判断、NFO 和图片写入链路；只修改文档。
- 重写 docs/metadata-providers.md 与 task_plan.md，并将审计报告的后续工作链接到统一方案。
- 明确待整改缺口：首批搜索结果不是完整候选集；电影来源标称时长尚未写入 NFO。
- 明确保护边界：模型及来源失败不进入 NFO 写入；单文件原子替换不代表整组输出事务回滚。
- 代码仍采用此前 116 个测试函数的验证结果，本次没有把文档修订表述为再次执行代码测试。
- 方案复核完成：已逐段追踪输入、来源、判断、写入和服务端读取边界；Markdown 代码块、本地链接及差异检查通过。仅修改 6 份文档，未改业务代码。

## 具名 LLM 与有序来源方案修订（2026-09-24）

- 用户澄清数据源是 TheTVDB 网页抓取；已有实现直接纳入配置，不新增同站重复来源。
- 用户确认来源按顺序逐个获取并判断，确认即停止，取代跨来源汇总判断设计。
- 目标配置新增 llms 具名实例与 scraper 刮削设置；分别引用提取和判断模型，保留 OpenAI 兼容协议与 Jev 的能力区别。
- 明确来源未匹配与执行错误的边界：none/低 Noul 可以继续下一来源，网络/鉴权/格式异常立即报错。
- 本轮只修订方案、计划和审计后续步骤；业务代码、当前运行配置示例和此前 116 个测试函数结果未改变，不宣称新行为已实现或测试通过。
- 目标配置示例的 JSON 语法、实例名称唯一性、任务引用和协议能力、来源顺序及文档链接已静态核对；新运行行为仍待实现与测试。

## 最终方案汇总（2026-09-24）

- 以 docs/metadata-providers.md 作为唯一设计依据，保留已确定的字段定义和配置示例，统一模型、来源、判断及输出职责。
- 完整流程图显式区分无候选、未确认、来源用尽、执行错误和输出错误，并以行为表逐分支静态核对。
- 六个待执行阶段与 task_plan.md 一致。仅修订文档，不修改业务代码、不重新宣称旧代码测试覆盖新方案。

## 四端 NFO 官方文档与源码核查（2026-09-24）

- 按用户限定方式核查，未部署或运行媒体服务器；本机 Docker 仅在用户限定前作只读可用性检查。
- 调用当前 nfo.Write 生成电影、节目、单集、特别篇四个合成样本，XML 解析和转义通过；保存在 docs/nfo-samples/。
- 核查 Kodi、Jellyfin、Emby 官方 NfoMetadata 插件与 Silo 的文件发现、解析和结果映射；报告见 docs/nfo-recognition-audit.md。
- 结论：普通媒体核心 NFO 可读；电影时长遗漏、语言标签无效、Silo 部分字段不保留与 Logo 命名缺口，以及原盘扫描/发现问题需要分别处理。未把单评分缺少默认属性误判为不能读取。
- 仅新增报告与样本并更新设计状态，没有修改业务代码；现有单元测试结果不代替四端运行验证。

## NFO 字段修复纳入方案（2026-09-24）

- 明确三项待实施修改：电影/单集来源标称时长映射、移除无效 NFO 语言标签、停止输出与库内数量不符的网站节目总数。
- 明确保留边界：来源语言与数量事实保留，单集季集坐标（包括零季）、当前对象网站编号和已可读的单条评分不改语义。
- 方案增加字段定义、删除理由、输入与预期 XML、回归及受控 CLI 验收；同步任务计划与专项核查报告。
- 本轮仅修改文档，未实施字段修复，也未将旧测试结果用作修复完成的证据。

## 完整优化实施启动

- 用户批准将可直接清理的残留纳入方案，并明确移除整套 Kodi JSON-RPC；保留原盘和人工设置入口。
- 按范围清理、模型/配置、来源调度/覆盖、NFO/输出、全链路验证顺序实施。
- 过滤回归先失败：扫描和监听均接受了关键词命中文件及自定义临时后缀文件；将使用同一排除规则修复。
- 此前完整暂存差异保存为 /tmp/metadata-before-full-optimization.patch；无提交、合并或推送。

### 步骤 1 验证

- 删除 Kodi JSON-RPC 12 个文件及全部接线、旧文件名规则 582 行和专用测试、闲置缓存/查询工具及规则技术标签。
- 保留并修复关键词与临时后缀过滤；红测先复现扫描/监听均未排除，接入共享规则后通过。
- go test ./... -count=1 -timeout=60s 全通过，记录 /tmp/metadata-step1-tests.log；独立复核进行中。
- 首次合并批量删除被自动审批判定范围不清而拒绝，未执行；补充用户原文授权并按明确功能分项、显式文件删除后获批，已完成且无待处理阻塞。
- 步骤 1 独立复核通过，无重要问题；阶段完成，进入模型与配置改造。

### 步骤 2 验证

- 具名模型CLI红测先失败：旧程序仍要求ai.*，不识别新配置。实现后双服务的地址、凭据、模型和任务隔离通过；未选Jev无需密钥。
- 模型列表、严格配置解析、提取器显式注入、scraper.NfoField迁移已实现。定向底层与消费端tests/vet通过。
- go test ./... -count=1 -timeout=60s 全通过，记录 /tmp/metadata-step2-tests.log；独立复核进行中。
- 步骤 2 独立复核通过；自审新增发现显式无效代理会直连的旧缺口，当前阶段补失败回归与严格代理错误处理，完成后再进入来源调度。
- 步骤 2 完成：代理红测复现非法代理触达目标，修复后零请求并报错；空代理和合法代理路由保留。窄复核通过，最终全量测试记录 /tmp/metadata-step2-final.log。

### 步骤 3 完成

- 顺序来源红测复现旧程序仍请求第二来源；实现后确认即停止、低Noul/none继续、同源节目与单集、缓存仍执行判断均通过。
- 两来源分页与标题集合覆盖、完整计数/无进展/254边界通过；缓存key与封装版本均为3。
- 独立复核发现搜索404误归未匹配；端到端红测复现访问后续源，修复后搜索404明确报错、详情404仍表示对象缺失，NFO不覆盖。窄复核通过。
- 全量测试通过：/tmp/metadata-step3-final.log；metadata、tmdb、thetvdb竞态及静态检查通过。

### 步骤 4 进行中

- NFO四类对象字段红测复现遗漏时长、无效语言、错误节目数量及Logo引用；修复后nfo/utils tests和vet通过。
- CLI回归同样先复现来源runtime=123没有落盘且输出languages；字段修复后的CLI验证通过。
- 原盘红测复现DVD入口缺失、内部普通扩展文件误派发、VIDEO_TS路径重复及蓝光根NFO缺失；正在修复。
- 步骤 4 完成：字段、原盘目录和Logo路径已修复；媒体根复核发现外部原盘祖先误判、全扫嵌套重复/错类、相对根和spec子根遗漏，均先复现再修复，窄复核通过。
- 最终阶段4整合回归通过：/tmp/metadata-step4-verified.log；修复后四类XML样本已更新，电影runtime=123、无languages/节目总量、特别篇零季保留。

### 最终审查修复波次

- 首轮最终race：160个测试函数、493个节点、15个测试包，0失败；vet与四个构建目标通过。该结果发生在最终审查新增边界回归之前，历史记录保存为 /tmp/metadata-before-final-review-fixes-tests.jsonl。
- 整体只读审查发现相对节目根跨阶段归属、TheTVDB畸形候选误归无结果、LLM顶层null被接受；并确认SOCKS握手需要有限超时以释放资源。已集中安排先红后绿修复，完成后重跑最终验证，不以此前通过结果代替。

## 本轮最终交付验证

- 最终整体审查四项全部关闭：相对节目根、TheTVDB畸形候选、顶层null提取、SOCKS停滞握手。每项有红绿证据和后续请求/NFO或连接释放回归；一次集中修复后的窄复核通过，无遗留重要问题。
- `go test -race ./... -count=1 -timeout=90s -json`：15个测试包、167个测试函数、519个测试节点（含子测试），0失败。最终记录 `/tmp/metadata-optimization-final-tests.jsonl`。
- `go vet ./...`：通过。
- macOS本机构建、Linux amd64、Linux arm64、Windows amd64构建：通过。产物 `/tmp/metadata-optimized-*`。
- 本机二进制 `-version` 入口：通过（Go 1.27.1）。
- 实际CLI回放：具名双OpenAI实例隔离、Jev/通用判断、逐来源停止、低匹配继续、搜索404/畸形候选/null提取停止、相对节目根mode3、人工引用及原NFO保护均通过。
- NFO四类实际生成样本已刷新；源事实不变，字段映射与配套路径符合已核查的读取契约。没有启动媒体服务器、调用真实模型账户或修改真实媒体。
- 方案、README、示例配置、四端核查和整改报告已同步到实现；无新旧配置并行入口。
- 代码、测试及文档全部保存在 `codex/metadata-providers` 隔离工作区；未提交、合并或推送。
