# 主分支复验与真实样本记录

验证日期：2026-09-24。实现提交 `6161a7e` 已快进合并到 `master`，本轮顺序为先提交合并，再在主分支复验，最后使用执行时的 config.json 验证样本。

## 主分支复验

- `go test -race ./... -count=1 -timeout=90s -json`：16 个测试包、197 个测试函数、526 个测试节点通过，零失败。
- `go vet ./...` 通过。
- macOS 本机、Linux amd64、Linux arm64、Windows amd64 构建通过。
- 已移除能力、预设媒体类型及样本名称特例检查通过；正式文档本地链接检查通过，修正一处指向旧扫描文件的链接。
- 实际 NFO 写入器生成本地电影、节目、特别篇样本并重新解析通过；四端对 local 唯一标识的接受或忽略行为见 [NFO 专项报告](nfo-recognition-audit.md#8-本地-ai-整理结果的读取边界)。未部署媒体服务器或执行播放验证。

证据保存在本地忽略目录 `.validation/final-master/`。测试使用受控模型及来源服务器，不能替代下面的真实服务验收。

## 输入准备

- res.txt 的原始视频副本共 5,419 个，路径集合核对一致，均为空文件；未复制原有 5,822 个 NFO 或其他元数据。
- 保留既定随机种子与 10 部节目，总计 570 个视频，没有重新抽样。
- res2.txt 的 529 个视频已经完整复现目录结构，忽略 5 个非视频条目，不执行清单中的脚本。
- 从当前 config.json 派生私有验证配置，只移除已删除的运行模式和媒体目录配置，调整验证日志参数。第一次运行保留原服务、模型、凭据、来源顺序、语言、门槛及过滤设置，原始配置哈希未变。首次服务失败后，用户明确要求将原始 config.json 的提取模型改为 gpt-5.6-luna，已只修改该模型名；Jev 和其他设置未改。随后仅在派生验证配置中将提取超时由 30 秒提高到 180 秒。
- 媒体范围通过一个 `--path` 传入隔离目录，不访问真实媒体库。

## 真实运行结果

第一部为固定抽样的 Candle.In.The.Tomb-The.Weasel.Grave，20 个视频全部被发现，排除 0 个。

1. 原配置提取模型 gpt-6-luna：HTTP 503，退出码 1，未生成 NFO，网站请求为 0。同服务最小诊断返回 `no_available_account`，消息为“无可用账号，请稍后重试”，未使用环境代理。
2. 用户将提取模型改为 gpt-5.6-luna：原 30 秒时限超时，退出码 1，仍未访问网站。
3. 派生验证配置将提取超时提高到 180 秒：约 81 秒完成，退出码 0，提取请求 1 次，本地整理请求 1 次。输出 1 个 tvshow 和 20 个 episodedetails，XML 均有效，但主来源全部为 local。

第三次运行的真实网站统计如下。请求次数表示 HTTP 实际尝试，重试为 0；候选缓存随后仅用于诊断重放。

| 来源 | HTTP 请求 | 搜索请求 | 批量页面/API 请求 | 单集详情补取 | 候选判断 |
| --- | ---: | ---: | ---: | ---: | ---: |
| TheTVDB | 5 | 2 | 2 | 0 | 1 |
| TMDb | 3 | 2 | 1 | 0 | 1 |

人工核查来源缓存，两个来源均取得《鬼吹灯之黄皮子坟》及所需 20 集：TheTVDB 节目记录编号 331775、TMDb 节目记录编号 72957。这些编号标识对应网站的节目对象，不是单集编号。

为调查为何转入本地整理，使用仅监听本机的诊断转发器捕获相同服务、模型、门槛的请求和响应。重放仍转入本地整理；Jev 服务返回模型 jev-1.13.0，其 TheTVDB 候选 Noul 匹配概率为 0.18（Choice 选择 c0），TMDb 候选为 0.24（Choice 选择 none），均未通过 0.8 的配置门槛。c0 只表示该次候选列表的第一个位置，不是网站作品编号。

两次判断分别消耗 11,853 和 20,359 输入 token；请求包含重复候选事实与每集详情，这只是后续分析输入组织的线索，尚未证明是低分的根因。未降低门槛、指定正确答案、切换判断模型或写入样本名称特例。

| 集合 | 本轮状态 |
| --- | --- |
| res.txt 第一部，20 个视频 | 程序输出完成，但准确性验收未通过：存在正确网站候选却全部输出 local |
| res.txt 其余 9 部，550 个视频 | 未运行：先解决第一部暴露的判断问题 |
| res2.txt，529 个视频 | 目录复现完成，真实模型验证未运行 |

证据目录均在本地忽略的 `.validation/` 下：`live-master-6161a7e/` 保存原 503，`live-master-6161a7e-gpt56/` 保存 30 秒超时，`live-master-6161a7e-gpt56-timeout180/` 保存完成运行和 XML 统计，`live-master-6161a7e-gpt56-capture/` 保存诊断请求与响应。私有配置和原始诊断产物不提交仓库。

## 继续条件

按用户规定，当前步骤准确性验证未通过时先暂停后续步骤。待明确整改 Jev 判断输入，或由用户选择使用通用模型判断后，重新验证同一部完整样本；不得更换样本、降低门槛或用文件名特例掩盖失败。

第一部准确性验收通过后，继续其余固定样本与 res2 集合，检查分类、匹配、季集、NFO 数量、图片与实际请求统计。当前不能宣称 10 部样本、真实模型准确率或 res2 本地整理已通过。
