# 电影与剧集多数据源优化计划

## 统一目标

仅支持电影和剧集，使用具名模型列表和独立刮削设置：指定通用 LLM 提取 → 按配置顺序获取当前来源候选 → 指定 LLM 判断 → 确认即输出并停止，未匹配才进入下一来源。目标使用方是 Jellyfin、Emby、Silo Server、Kodi。

完整契约及字段定义以 [优化方案](docs/metadata-providers.md) 为准；本文只记录执行阶段，避免多份并行设计。

## 已完成

- [x] 审计附件与原项目，解耦来源、提取、判断和输出。
- [x] 实现 TMDb 和 TheTVDB HTML 来源、同源节目与单集候选、人工约束及事实缓存。
- [x] 完成基线的单份模型配置及跨来源汇总判断，移除匹配/搜索模式与规则、首条回退，明确禁止模型生成事实。
- [x] 删除 ffmpeg、ffprobe 和音乐视频处理，以及配置、扫描、监听、启动和 Kodi 刷新入口。
- [x] 完成最近一次代码验证：116 个测试函数，race、vet、三平台构建和真实 CLI 的受控回放通过。
- [x] 汇总最终方案：具名多 LLM、独立刮削配置、有序来源处理、NFO 输出和六阶段验收；逐分支核对无候选、拒绝、执行错误及输出失败边界。

## 当前实施步骤

- [x] 1. 范围清理：删除整套 Kodi JSON-RPC 和已确认死代码，修复关键词/临时后缀过滤；验证扫描、监听及当前输出链路。
- [x] 2. 模型与配置：具名 llms、独立 scraper、实例注入、旧入口拒绝，验证凭据和任务能力隔离。
- [x] 3. 来源调度：逐来源获取/判断/确认即停止，完善分页与查询覆盖，验证超限与执行错误边界。
- [x] 4. NFO 与输出：落实字段修复、配套图片命名及原盘扫描/发现路径，按官方源码契约和受控 CLI 验证。
- [x] 5. 全链路检查：全量测试、race、vet、跨平台构建、独立审查、文档和实际示例同步。

每一步先验证并汇报，通过且不存在关键歧义后再继续。仅使用临时媒体副本和受控服务，不在真实库执行删除或媒体服务器控制。四端验收按用户要求采用官方文档及源码核对。真实账户模型准确率和部署网络可达性没有凭据或指定环境时单独记录，不用受控回放代替。

## 当前工作状态

- 工作区：`/Users/abc/.codex/worktrees/metadata-providers/kodi-metadata-tmdb-cli`。
- 分支：`codex/metadata-providers`；此前代码改造已暂存，尚未提交、合并或推送；本轮已完成已确认方案的代码实现及本地验证。
- 本轮实施前快照：/tmp/metadata-before-full-optimization.patch；不访问真实媒体库。
- 删除媒体工具前的完整差异：`/tmp/metadata-before-movie-show-only.patch`；代码验证记录见 [progress.md](progress.md)。
